package com.googlecode.coss.common.core.orm.mybatis.query;

public enum Condition {
    AND,
    OR,
    NOT
    //, BETWEEN_AND

    /*
     * AND("and"), OR("or"), NOT("not"), BETWEEN_AND("between_and"); private
     * final String conditionName; private Condition(String conditionName) {
     * this.conditionName = conditionName; }
     */
}
